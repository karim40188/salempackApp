import React from 'react';

const Login = () => {
  return (
    <div style={{
      height: '100vh',
      width: '100vw',
      background: 'red',
      color: '#fff',
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
    }}>
      Login Page
    </div>
  );
};

export default Login;
